Test the state machine with injected test inputs. For each state transition, attempt at least 3 invalid state transitions and 2 valid state transitions. Use an outside resource to come up with invalid state transitions; e.g. a non-software and non-controls member must be made aware of the intended conditions for each state transition, then come up with at least three invalid state transitions according to the state transition requirement. Verify the state machine only transitions from one state to another for valid transition inputs.